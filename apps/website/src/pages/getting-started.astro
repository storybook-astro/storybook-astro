---
import Layout from '../layouts/Layout.astro';
import { readFileSync } from 'node:fs';
import { resolve } from 'node:path';
import { Marked } from 'marked';

// Read and parse the Getting Started guide (lives at repo root docs/)
const guidePath = resolve(process.cwd(), '../../docs/GETTING_STARTED.md');
const guideMarkdown = readFileSync(guidePath, 'utf-8');
const marked = new Marked();
const guideContent = marked.parse(guideMarkdown);

---

<Layout title="Getting Started - Storybook Astro">
  <div class="guide-content" set:html={guideContent} />
</Layout>

<style>
  .guide-content {
    line-height: 1.7;
  }

  .guide-content :global(h1) {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }

  .guide-content :global(h2) {
    font-size: 1.75rem;
    margin-top: 2.5rem;
    margin-bottom: 1rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid #21262d;
  }

  .guide-content :global(h3) {
    font-size: 1.35rem;
    margin-top: 1.5rem;
    margin-bottom: 0.75rem;
  }

  .guide-content :global(p) {
    margin-bottom: 1rem;
    color: #c9d1d9;
  }

  .guide-content :global(ul),
  .guide-content :global(ol) {
    margin-bottom: 1rem;
    padding-left: 2rem;
  }

  .guide-content :global(li) {
    margin-bottom: 0.5rem;
    color: #c9d1d9;
  }

  .guide-content :global(a) {
    color: #58a6ff;
  }

  .guide-content :global(code) {
    background: rgba(110, 118, 129, 0.2);
    color: #e6edf3;
    padding: 0.15em 0.4em;
    border-radius: 4px;
    font-size: 0.9em;
  }

  .guide-content :global(pre) {
    background: #161b22;
    border: 1px solid #30363d;
    border-radius: 8px;
    padding: 1rem;
    overflow-x: auto;
    margin: 1rem 0;
  }

  .guide-content :global(pre code) {
    background: none;
    padding: 0;
    border-radius: 0;
    color: #c9d1d9;
    font-size: 0.875rem;
  }

  .guide-content :global(strong) {
    color: #e6edf3;
  }

  .guide-content :global(blockquote) {
    border-left: 3px solid #30363d;
    padding-left: 1rem;
    margin: 1rem 0;
    color: #8b949e;
    font-style: italic;
  }

  .guide-content :global(hr) {
    border: none;
    border-top: 1px solid #21262d;
    margin: 2rem 0;
  }

  .guide-content :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: 1rem 0;
  }

  .guide-content :global(th),
  .guide-content :global(td) {
    border: 1px solid #30363d;
    padding: 0.75rem;
    text-align: left;
    color: #c9d1d9;
  }

  .guide-content :global(th) {
    background: #161b22;
    font-weight: 600;
    color: #f0f6fc;
  }
</style>
